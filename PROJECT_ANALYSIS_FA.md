# 🛡️ مستند جامع تحلیل معماری، فایل‌ها و خط‌به‌خط سیستم Nyx Panel (نسخه ۲.۲.۰)
> **توسعه‌یافته توسط تیم امنیتی ساینت (Cynet Security Team)**  
> این سند راهنمای ۰ تا ۱۰۰، دقیق و فنی برای تحلیل، نگهداری، بازطراحی و توسعه تمامی بخش‌های پروژه **Nyx Panel** است.

---

## 📑 فهرست مطالب
1. [معرفی کلی و هدف پروژه](#1-معرفی-کلی-و-هدف-پروژه)
2. [معماری کلی سیستم (Architecture Overview)](#2-معماری-کلی-سیستم-architecture-overview)
3. [ساختار فایل‌ها و پوشه‌های پروژه (File Tree)](#3-ساختار-فایل‌ها-و-پوشه‌های-پروژه-file-tree)
4. [تحلیل کامل لایه دیتابیس (Prisma & SQLite)](#4-تحلیل-کامل-لایه-دیتابیس-prisma--sqlite)
5. [تحلیل عمیق بک‌اند و سرور اصلی (`backend/src/index.ts`)](#5-تحلیل-عمیق-بک‌اند-و-سرور-اصلی-backendsrcindexts)
6. [تحلیل ماژول ارتباط با هسته Xray (`backend/src/xray/`)](#6-تحلیل-ماژول-ارتباط-با-هسته-xray-backendsrcxray)
   - `configGenerator.ts`
   - `downloader.ts`
7. [تحلیل سرویس‌های بک‌اند (`backend/src/services/`)](#7-تحلیل-سرویس‌های-بک‌اند-backendsrcservices)
   - `subscriptionService.ts`
   - `autoFailoverService.ts`
   - `warpService.ts`
   - `backupService.ts`
   - `xrayStatsService.ts`
   - `tunnelManager.ts`
   - `telegramBot.ts`
8. [تحلیل فرانت‌اند و رابط کاربری (`frontend/src/`)](#8-تحلیل-فرانت‌اند-و-رابط-کاربری-frontendsrc)
   - `App.vue`, `main.ts`, `style.css`, `i18n.ts`
   - `DashboardView.vue`
   - `UsersView.vue`
   - `InboundsView.vue`
   - `NodesView.vue`
   - `TunnelsView.vue`
   - `SettingsView.vue`
   - `SubUserView.vue`
   - `LoginView.vue`, `ToastNotification.vue`, `utils/clipboard.ts`
9. [تحلیل اسکریپت‌های نصب و استقرار (`install.sh`, `install.ps1`, `uninstall.sh`)](#9-تحلیل-اسکریپت‌های-نصب-و-استقرار-installsh-installps1-uninstallsh)
10. [جریان‌های داده و ارتباطات فنی (Data Flows)](#10-جریان‌های-داده-و-ارتباطات-فنی-data-flows)
11. [نکات امنیتی، ضد فیلترینگ و دور زدن DPI اپراتورها](#11-نکات-امنیتی-ضد-فیلترینگ-و-دور-زدن-dpi-اپراتورها)

---

## 1. معرفی کلی و هدف پروژه

**Nyx Panel** یک پنل مدیریت پیشرفته، سبک و متمرکز بر دور زدن فیلترینگ و اختلالات شبکه در ایران است. این پروژه بر پایه پروتکل‌های مدرن **Xray-core** (به‌ویژه `VLESS + REALITY`، `Packet Fragment` و سوئیچ پویای SNI) پیاده‌سازی شده و برخلاف پنل‌های سنگینی مثل Marzban و 3x-ui، مصرف رم بسیار پایینی (حدود ۷۰ تا ۱۰۰ مگابایت) دارد و بر روی هر دو سیستم‌عامل **لینوکس (Ubuntu/Debian/CentOS)** و **ویندوز سرور (Windows Server 2016 تا 2022 / ویندوز 10 و 11)** به‌صورت کاملاً نیتیو اجرا می‌شود.

### مهم‌ترین قابلیت‌های کلیدی:
1. **پروتکل VLESS + REALITY با کلیدهای امنیتی X25519 و Vision Flow:** شبیه‌سازی کامل هندشیک TLS به سمت وب‌سایت‌های خارجی بدون نیاز به دامنه شخصی و سرتیفیکیت SSL.
2. **قابلیت Packet Fragmentation:** تکه‌تکه کردن پکت‌های اولیه ارتباط به قطعات ۱۰۰ تا ۲۰۰ بایتی برای شکستن الگوهای هوش مصنوعی DPI در اپراتورهای همراه اول، ایرانسل و مخابرات.
3. **دیمن هوشمند Auto-Failover SNI:** پایش زنده و خودکار سلامت دامنه‌های SNI در هر ۶۰ ثانیه و جابجایی خودکار دامنه فیلترشده با دامنه‌های وایت‌لیست (مانند بانک‌ها، ابر آروان یا دامنه‌های شرکتی) بدون نیاز به تغییر لینک سابسکریپشن کلاینت‌ها.
4. **خروجی ۱-کلیکه Cloudflare WARP:** رجیستر مستقیم اکانت WireGuard از طریق API کلاینت رسمی کلودفلر برای رفع تحریم سایت‌های هوش مصنوعی (ChatGPT, OpenAI)، استریمینگ (Netflix, Spotify) و مخفی‌سازی کامل IP اصلی VPS.
5. **بکاپ‌گیری اتوماتیک و ریستور ۱-کلیکه در تلگرام:** ارسال خودکار فایل دیتابیس `.db` هر ۲۴ ساعت به پیوی ادمین در تلگرام همراه با هش امنیتی SHA-256 و امکان بازگردانی کل دیتابیس در کمتر از ۳ ثانیه تنها با آپلود فایل در ربات.
6. **ربات تلگرام کاملاً دکمه‌ای (Wizard-driven):** مدیریت ۰ تا ۱۰۰ اینباندها، کاربران، تست زنده SNI، ایجاد اسکریپت تونل و صدور لینک بدون نیاز به کامندهای متنی.
7. **صفحه وب اختصاصی برای هر کاربر (`/subinfo/:uuid`):** مشاهده آنلاین حجم مصرفی، روزهای باقیمانده، سقف دستگاه، کدهای QR و کانفیگ‌های Sing-Box، Clash Meta و VLESS.
8. **ژنراتور اتوماتیک اسکریپت تونل بین ایران و خارج:** ساخت اسکریپت‌های ۱-کلیکه برای متدهای Gost v3، Rathole، ICMP Ping Tunnel، White DNS Tunnel و IPv6 Relay.

---

## 2. معماری کلی سیستم (Architecture Overview)

معماری سیستم به صورت ۳ لایه تفکیک شده (Frontend - Backend - Engine):

```
┌────────────────────────────────────────────────────────────────────────┐
│                        Front-End Single Page App                       │
│    Vue 3 + Vite + Tailwind CSS + Lucide Icons + QRCode.vue + i18n     │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │ HTTP REST API (Bearer Token)
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│                        Node.js Express Backend                         │
│  - REST API Routes (/api/users, /api/inbounds, /api/stats, etc.)       │
│  - Background Daemons (TrafficSync, AutoFailover, BackupDaemon)        │
│  - Telegram Bot Engine (Polling, State Machine, Admin Wizard)          │
│  - System Service Manager (Process Spawner, Firewall/iptables)         │
└───────────────┬──────────────────────────┬─────────────────────────────┘
                │                          │
                ▼                          ▼
┌───────────────────────────────┐ ┌──────────────────────────────────────┐
│     SQLite DB (Prisma ORM)    │ │              Xray-Core               │
│ - User (uuid, limits, expiry) │ │ - Inbounds (VLESS REALITY, TCP/WS)   │
│ - Inbound (keys, sni, ports)  │ │ - gRPC Stats API (127.0.0.1:10085)   │
│ - Node (Iran/Kharej, IP)      │ │ - Outbounds (Freedom, Wireguard WARP)│
│ - SystemSetting (Bot, Config) │ │ - Routing Rules (Direct, Torrent Blk)│
└───────────────────────────────┘ └──────────────────────────────────────┘
```

---

## 3. ساختار فایل‌ها و پوشه‌های پروژه (File Tree)

```
xray/
├── README.md                 # مستند معرفی و آموزش به زبان فارسی
├── README_EN.md              # مستند پروژه به زبان انگلیسی
├── LICENSE                   # مجوز پروژه (MIT License)
├── install.sh                # اسکریپت نصب خودکار برای سیستم‌عامل لینوکس
├── install.ps1               # اسکریپت نصب خودکار برای ویندوز سرور (PowerShell)
├── uninstall.sh              # اسکریپت حذف کامل و پاکسازی پنل
├── mainlogo.png / logo_trans # بنرها و لوگوهای رسمی پنل
├── backend/                  # سورس بک‌اند
│   ├── package.json          # وابستگی‌های Node.js بک‌اند
│   ├── tsconfig.json         # تنظیمات تایپ‌اسکریپت بک‌اند
│   ├── prisma/
│   │   ├── schema.prisma     # مدل‌های دیتابیس SQLite
│   │   └── dev.db            # فایل دیتابیس SQLite ذخیره‌سازی اطلاعات
│   ├── bin/                  # باینری‌های اجرایی
│   │   ├── xray / xray.exe   # فایل اجرایی هسته Xray-core
│   │   ├── config.json       # کانفیگ نهایی جنریت‌شده Xray
│   │   ├── geoip.dat         # دیتابیس روتینگ آی‌پی کشورها
│   │   └── geosite.dat       # دیتابیس روتینگ دامنه‌ها
│   ├── backups/              # محل ذخیره محلی بکاپ‌های خودکار دیتابیس
│   └── src/
│       ├── index.ts          # سرور اصلی اکسپرس، روت‌های API و چرخه حیات
│       ├── xray/
│       │   ├── configGenerator.ts  # ساخت config.json برای Xray و تولید کلیدهای X25519
│       │   └── downloader.ts       # دانلود و اعتبارسنجی خودکار باینری Xray از گیت‌هاب
│       └── services/
│           ├── autoFailoverService.ts # پایش لایو SNI و سوئیچ خودکار در زمان مسدودی
│           ├── backupService.ts       # ایجاد بکاپ، هش‌گذاری و ریستور دیتابیس
│           ├── subscriptionService.ts # تولید لینک‌های VLESS، Base64، Sing-box و Clash
│           ├── telegramBot.ts         # ربات تلگرام جامع ادمین و کلاینت
│           ├── tunnelManager.ts       # ساخت اسکریپت‌های Gost، Rathole، ICMP و DNS Tunnel
│           ├── warpService.ts         # اتصال به API کلودفلر و تنظیم WireGuard WARP
│           └── xrayStatsService.ts    # مانیتورینگ مصرف ترافیک و انقضای کاربران با gRPC
└── frontend/                 # سورس فرانت‌اند
    ├── package.json          # پکیج‌های Vue 3، Tailwind و Vite
    ├── vite.config.ts        # تنظیمات بیلد Vite و پروکسی
    ├── tailwind.config.js    # تنظیمات استایل‌های سایبرپانک و متغیرهای رنگی
    ├── index.html            # پوسته اصلی HTML با متادیتاها
    └── src/
        ├── main.ts           # نقطه ورود Vue 3
        ├── App.vue           # کامپوننت ریشه، نوار ناوبری، تب‌ها و احراز هویت
        ├── style.css         # استایل‌های سراسری، فونت وزیرمتن و افکت‌های شیشه‌ای
        ├── i18n.ts           # سیستم دوزبانه کامل (فارسی و انگلیسی)
        ├── utils/
        │   └── clipboard.ts  # تابع امن کپی در کلیپ‌بورد با پشتیبانی از مرورگرها و HTTP
        └── components/
            ├── DashboardView.vue   # صفحه آمار لایو سرور، رم، پردازنده، وضعیت هسته و پروتکل‌ها
            ├── InboundsView.vue    # مدیریت پورت‌ها، تست آنلاین SNI و تنظیمات اینباندها
            ├── UsersView.vue       # مدیریت کاربران، حجم، تاریخ انقضا و دریافت لینک‌ها
            ├── NodesView.vue       # مدیریت سرورهای چندگانه و نودهای ایران/خارج
            ├── TunnelsView.vue     # جنراتور اسکریپت‌های اتصال تانل ایران و خارج
            ├── SettingsView.vue    # تنظیمات ربات تلگرام، WARP، بکاپ و راه‌اندازی مجدد
            ├── SubUserView.vue     # صفحه عمومی کاربر برای مشاهده وضعیت حجم و بارکدها
            ├── LoginView.vue       # صفحه لاگین شیشه‌ای ادمین
            └── ToastNotification.vue # سیستم نمایش هشدارهای شناور (Success/Error/Info)
```

---

## 4. تحلیل کامل لایه دیتابیس (Prisma & SQLite)

فایل `backend/prisma/schema.prisma` قلب مدل‌سازی داده‌های پنل است:

```prisma
datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}
```

### مدل‌های داده:

1. **`User` (کاربران و سابسکرایبرها):**
   - `id`: شناسه یکتا (UUID).
   - `username`: نام کاربری منحصر‌به‌فرد.
   - `uuid`: شناسه کاربری در پروتکل VLESS برای احراز هویت در Xray.
   - `email`: ایمیل کاربر (اختیاری، معادل username برای ثبت در متادیتاهای ترافیک Xray).
   - `dataLimitGb`: سقف ترافیک به گیگابایت (مقدار 0 یعنی نامحدود).
   - `usedDataBytes`: مجموع بایت‌های مصرف‌شده ترافیک (از نوع `BigInt` برای پشتیبانی از ترابایت بدون سرریز شدن عدد).
   - `expireDate`: تاریخ انقضای حساب (`DateTime` یا null برای بدون انقضا).
   - `status`: وضعیت کاربر (`ACTIVE`, `EXPIRED`, `DISABLED`).
   - `maxDevices`: محدودیت اتصال همزمان (پیش‌فرض ۲ کاربر).
   - `notes`: توضیحات ادمین.
   - `createdAt`, `updatedAt`: زمان ساخت و ویرایش.

2. **`Node` (سرورها و نودهای شبکه):**
   - `id`, `name`: شناسه و نام نود (مثلاً Asiatech-Relay).
   - `type`: نوع نود (`KHAREJ` به عنوان مستر یا `IRAN_RELAY`).
   - `ip`: آدرس IP نود.
   - `apiPort`: پورت gRPC و ارتباطی (پیش‌فرض 10085).
   - `status`: وضعیت نود (`ONLINE`, `OFFLINE`).
   - `isMaster`: مشخص‌کننده سرور اصلی.
   - `tunnelType`: متد تونل انتخابی (`GOST`, `RATHOLE`, `ICMP`, `DNS`, `NONE`).
   - `tunnelPort`, `tunnelSecret`: پورت و کلید رمزنگاری تونل.

3. **`Inbound` (اینباندها و درگاه‌های ورودی):**
   - `id`, `uuid`: شناسه اینباند.
   - `remark`: نام نمایشی کانفیگ (مثلاً `⚡ Cynet-Default-VIP`).
   - `protocol`: پروتکل انتقال (پیش‌فرض `vless`).
   - `port`: پورت شنود ورودی روی سرور (مانند 443, 8443, 2083).
   - `network`: لایه انتقال شبکه (`tcp`, `ws`, `grpc`).
   - `security`: نوع امنیت (`reality`, `tls`, `none`).
   - `sni`: دامنه ماسک فریبنده TLS (مانند `ebanking.banksepah.ir` یا `yahoo.com`).
   - `privateKey`, `publicKey`: جفت‌کلید رمزنگاری X25519 با طول ۳۲ بایت Base64URL.
   - `shortId`: شناسه تصادفی هگز دسیمال برای افزایش امنیت REALITY.
   - `enableFragment`: فعال‌بودن تکه‌تکه کردن بسته‌های TCP/TLS.
   - `fragmentLength`: بازه طولی فرگمنت (پیش‌فرض `100-200` بایت).
   - `fragmentInterval`: بازه زمانی تاخیر فرگمنت (پیش‌فرض `10-20` میلی‌ثانیه).
   - `dataLimitGb`, `usedDataBytes`, `expireDate`, `maxDevices`, `enabled`.

4. **`SystemSetting` (تنظیمات سراسری سیستم به صورت Key-Value):**
   - `key`: کلید تنظیم (مثلاً `BOT_TOKEN`, `ADMIN_CHAT_ID`, `WARP_CONFIG`).
   - `value`: مقدار رشته‌ای یا JSON ساختاریافته.

---

## 5. تحلیل عمیق بک‌اند و سرور اصلی (`backend/src/index.ts`)

فایل `index.ts` نقطه اتصال تمامی ماژول‌ها و سرویس‌هاست و بخش‌های زیر را کنترل می‌کند:

### الف) رفع مشکل سریالایز `BigInt` در جاوااسکریپت:
در خطوط ۷۶ تا ۷۸، متد `toJSON` برای پروتوتایپ `BigInt` اضافه شده است تا خطای رایج اکسپرس هنگام ارسال حجم بایت‌های بالا رخ ندهد:
```typescript
(BigInt.prototype as any).toJSON = function () {
  return this.toString();
};
```

### ب) تشخیص هوشمند IP عمومی سرور (`autoDetectPublicIp` & `getPublicHost`):
در خطوط ۳۵ تا ۶۱، سرور به‌صورت دوره‌ای هر ۵ دقیقه یکبار از طریق سرویس `api.ipify.org` آی‌پی عمومی سرور را استخراج و کش می‌کند. اگر ریکوئست کلاینت شامل هدر `Host` یا دامنه باشد، سیستم از آن برای تولید لینک‌های سابسکریپشن استفاده می‌کند تا کاربر بدون نیاز به تنظیم دستی، لینک صحیح متناسب با دامنه یا آی‌پی متصل‌شده را دریافت کند.

### ج) امنیت و سیستم احراز هویت بدون وابستگی (Token-based Auth):
در خطوط ۶۳ تا ۱۱۸، ادمین با متغیرهای محیطی `ADMIN_USER` و `ADMIN_PASS` احراز هویت می‌شود. با لاگین موفق در مسیر `/api/auth/login`، یک توکن تصادفی ۳۲ بایتی با ماژول امنیتی `crypto` تولید شده و در حافظه `activeTokens` ثبت می‌گردد. تمام روت‌های تحت `/api/` (به جز لینک‌های عمومی سابسکریپشن کلاینت `/api/sub/`، `/api/subinfo/` و تست SNI) تحت حفاظت این میدل‌ویر قرار دارند.

### د) روت‌های اصلی API:
- `GET /api/stats/dashboard`: جمع‌آوری درصد رم و پردازنده واقعی با ماژول `os` سیستم‌عامل، آپ‌تایم سرور، پینگ لحظه‌ای، تعداد کل کاربران فعال/منقضی و وضعیت فرآیند Xray.
- `GET / POST / PATCH / DELETE /api/users`: ساخت، ویرایش و حذف کاربران و اعمال تغییرات بلادرنگ روی Xray.
- `GET /api/users/:id/configs`: تولید خودکار تمامی لینک‌ها و فرمت‌های VLESS، Sing-Box و Clash Meta برای کاربر بر اساس تمامی اینباندهای فعال.
- `GET / POST / PATCH / DELETE /api/inbounds`: تولید کلیدهای X25519، مدیریت پورت‌ها، تغییر SNI و فعال‌سازی اینباندها.
- `GET /api/sni/test`: تست واقعی و آنلاین هندشیک TLS 1.3 در لایه سوکت TCP روی پورت ۴۴۳ به همراه سنجش لتنسی و استخراج صادرکننده گواهی SSL (`cert.issuer`).
- `GET / POST /api/sni/auto-failover/*`: مشاهده وضعیت و اجرای دستی دیمن پایش و سوئیچ هوشمند SNI.
- `GET / POST /api/warp/*`: رجیستر اکانت جدید کلودفلر، فعال‌سازی خروجی WireGuard و تعیین مود روتینگ (ALL / SANCTIONED).
- `GET / POST /api/backup/*`: دانلود مستقیم دیتابیس SQLite یا ارسال فوری آن به تلگرام ادمین.
- `POST /api/system/restart` و `POST /api/system/reload-xray`: کنترل سرویس و بارگذاری مجدد هسته Xray بدون نیاز به ورود به SSH.
- `POST /api/nodes/tunnel-script`: دریافت پارامترها و بازگرداندن اسکریپت‌های نصب تونل‌های ایران و خارج.
- `GET /api/sub/:uuid` و `GET /api/subinfo/:uuid`: روت‌های عمومی سابسکریپشن با فرمت‌های `base64`، `singbox` و `clash`.

### هـ) فرآیند مدیریت سرویس Xray (`reloadXrayService`):
در خطوط ۸۰۶ تا ۹۰۸:
1. دریافت تمامی اینباندهای فعال و کاربران معتبر از دیتابیس.
2. دریافت تنظیمات فعال WARP از دیتابیس.
3. تولید فایل استاندارد `config.json` از طریق `generateXrayJsonConfig`.
4. ذخیره فایل در مسیر `backend/bin/config.json`.
5. باز کردن خودکار پورت‌های اینباند در فایروال لینوکس با استفاده از دستورات `iptables` و `ufw`.
6. در محیط لینوکس، کشتن فرآیندهای قبلی مسدودکننده پورت (مثل پورتهای وب یا gRPC) و خاتمه دادن امن به پروسه قبلی Xray.
7. اجرای `xray run -config config.json` به عنوان پروسه فرزند (`ChildProcess`) و پایش خروجی‌های لاگ `stdout` و `stderr` با چک کردن PID و متغیر `isXrayRunning`.

---

## 6. تحلیل ماژول ارتباط با هسته Xray (`backend/src/xray/`)

### ۱. فایل `downloader.ts` (دانلود و اعتبارسنجی باینری):
- **تابع `ensureXrayBinary()`:**
  1. پوشه `backend/bin` را بررسی می‌کند. اگر باینری `xray` (در لینوکس) یا `xray.exe` (در ویندوز) از قبل موجود باشد و دستور `xray version` بدون خطا خروجی دهد، همان باینری تأیید می‌شود.
  2. اگر فایل زیپ لوکال `xray.zip` در پوشه قرار داشته باشد، آن را اکسترکت می‌کند.
  3. در صورت عدم وجود باینری، از طریق API رسمی گیت‌هاب `XTLS/Xray-core` آخرین نسخه و متناسب با معماری سرور (`linux-64`, `linux-arm64-v8a`, `windows-64`) را شناسایی می‌کند.
  4. دانلود فایل با استفاده از ۴ میرور ضد تحریم (مانند `ghproxy.net` و `mirror.ghproxy.com`) انجام شده و پس از اکسترکت و تست اعتبارسنجی اجرا، مجوز اجرایی `chmod +x` اعمال می‌گردد.

### ۲. فایل `configGenerator.ts` (ژنراتور پیکربندی و کلیدها):
- **تابع `generateX25519Keypair()`:**
  - تلاش اول: اجرای دستور `xray x25519` باینری برای تولید استاندارد جفت‌کلید عمومی و خصوصی.
  - تلاش فال‌بک (Fallback): در صورتی که باینری در دسترس نباشد، با استفاده از ماژول بومی `crypto.generateKeyPairSync('x25519')` در Node.js، کلیدهای استاندارد ۳۲ بایتی در فرمت `base64url` تولید می‌شوند.
- **تابع `generateXrayJsonConfig()`:**
  - بخش `api`: باز کردن پورت اینترنال `127.0.0.1:10085` با پروتکل `dokodemo-door` و تگ `api` برای اتصال به سرویس‌های آمارگیری HandlerService, StatsService, LoggerService.
  - بخش `inbounds`: ساخت اینباندهای VLESS. برای هر کاربر و هر اینباند، آیدی اختصاصی و جریان `flow: "xtls-rprx-vision"` ثبت می‌شود. در ساختار `realitySettings`، دامنه‌های هدف (`dest` و `serverNames`)، `privateKey`، `shortIds` و متدهای اسنیفینگ `sniffing` برای پکت‌های http, tls, quic تزریق می‌گردند.
  - بخش `outbounds`: ساخت روت‌های `direct` (با پروتکل freedom) و `blocked` (با پروتکل blackhole).
  - تزریق پروتکل `wireguard` مربوط به Cloudflare WARP در صورت فعال بودن.
  - بخش `routing`: جلوگیری از مصرف ترافیک روی تورنت با رول `protocol: ["bittorrent"] -> blocked` و ارسال دامنه‌های تحریمی هوش مصنوعی و فیلم به سمت WARP.

---

## 7. تحلیل سرویس‌های بک‌اند (`backend/src/services/`)

### ۱. `subscriptionService.ts` (سرویس تولید کانفیگ و سابسکریپشن)
این سرویس وظیفه تبدیل اطلاعات دیتابیس به فرمت‌های استاندارد نرم‌افزارهای کلاینت مثل v2rayN, v2rayNG, Streisand, Shadowrocket, Sing-Box, NekoBox, Clash Meta, Stash را دارد:
- `generateVlessLink`: تولید لینک مستقیم با اسکیما `vless://UUID@IP:PORT?...#Remark`.
- `generateBase64Sub`: جمع‌آوری تمام لینک‌های VLESS فعال و اینکود آن‌ها به فرمت Base64.
- `generateSingBoxJson`: تولید فایل استاندارد JSON شامل روتینگ‌های پکت برای اپلیکیشن Sing-Box و Nekobox با رول دور زدن سایت‌های داخلی ایران (`geoip: ["ir"] -> direct`).
- `generateClashYaml`: تولید فایل YAML با گروه‌بندی اتوماتیک URL-Test و فیک‌آی‌پی Fake-IP برای نرم‌افزارهای کلش متا.

### ۲. `autoFailoverService.ts` (سامانه پایش لایو و سوئیچ خودکار SNI)
یکی از پیشرفته‌ترین بخش‌های پنل برای مقابله با فیلترینگ شدید اپراتورها:
- **مکانیزم عملکرد:**
  1. استخر دامنه‌های پشتیبان و وایت‌لیست (`FALLBACK_SNI_POOL`) شامل دامنه‌هایی مانند `ebanking.banksepah.ir`، `bmi.ir`، `arvancloud.ir`، `divar.ir`، `digikala.com`، `pypi.org`، `archive.ubuntu.com` و `yahoo.com`.
  2. متد `testSniDomain`: ایجاد سوکت TLS مستقیم روی پورت ۴۴۳ به دامنه مقصد با تایم‌اوت ۳ ثانیه‌ای.
  3. دیمن مانیتورینگ (`startDaemon`): هر ۶۰ ثانیه یک‌بار تمامی اینباندهای فعال را بررسی می‌کند. اگر هندشیک TLS دامنه‌ای با خطا یا تایم‌اوت مواجه شود، سیستم بدون درنگ بهترین دامنه سالم از استخر را جایگزین آن کرده، دیتابیس را به‌روزرسانی می‌کند و هسته Xray را بلافاصله ریلود می‌نماید.
  4. ارسال پیام هشدار فوری به تلگرام ادمین به همراه میزان لتنسی دامنه جدید.
  5. **نکته طلایی:** کلاینت‌ها بدون نیاز به تعویض لینک، به دلیل تغییر داینامیک در سرور، اتصالشان پایدار باقی می‌ماند.

### ۳. `warpService.ts` (مدیریت خروجی Cloudflare WARP)
- تعامل مستقیم با API رسمی اپلیکیشن کلودفلر: `POST https://api.cloudflareclient.com/v0a2158/reg`
- با ایجاد یک کلید X25519 و ارسال به کلودفلر، آی‌پی‌های اختصاصی IPv4 (در ساب‌نت `172.16.0.0/32`) و IPv6 و پابلیک‌کی کلاینت دریافت می‌شود.
- در صورت بروز محدودیت روی API، پروفایل لوکال پیش‌فرض WireGuard جایگزین می‌شود.
- پشتیبانی از دو حالت روتینگ:
  - `ALL`: عبور ۱۰۰٪ ترافیک خروجی کل سرور از شبکه WireGuard.
  - `SANCTIONED`: عبور انتخابی ترافیک سایت‌های هوش مصنوعی (OpenAI, ChatGPT) و رسانه (Netflix, Spotify).

### ۴. `backupService.ts` (موتور پشتیبان‌گیری و بازگردانی دیتابیس)
- `createBackup`: کپی ایمن و اتمیک از فایل `prisma/dev.db`، ثبت تاریخ و ساخت هش SHA-256 برای جلوگیری از دستکاری داده‌ها.
- `restoreBackup`: بازگردانی سریع دیتابیس در کمتر از ۳ ثانیه. ابتدا کلاینت پریزما دیسکانکت شده تا قفل فایل SQLite آزاد شود، بکاپ جایگزین شده، پریزما ری‌کانکت می‌شود و هسته Xray مجدداً ریلود می‌گردد. در صورت بروز هرگونه خطا، مکانیزم Rollback فایل پشتیبان قبلی را برمی‌گرداند.
- `sendBackupToTelegram`: ارسال فایل `.db` به صورت سند مستقیماً به چت ادمین در تلگرام.
- `startBackupDaemon`: دیمن خودکار ارسال روزانه بکاپ هر ۲۴ ساعت یکبار.

### ۵. `xrayStatsService.ts` (همگام‌سازی ترافیک و مدیریت انقضا با gRPC)
- پایش منظم هر ۲۰ ثانیه یک‌بار از طریق فراخوانی دستور:
  `xray api statsquery --server=127.0.0.1:10085 -reset=true`
- تفکیک ترافیک آپلود و دانلود هر کاربر از الگوی `user>>>username>>>traffic>>>uplink/downlink`.
- اضافه کردن مصرف به فیلد `usedDataBytes` در جدول دیتابیس.
- تغییر خودکار وضعیت کاربر به `EXPIRED` در صورت عبور از سقف حجم (`dataLimitGb`) یا رسیدن به تاریخ انقضا (`expireDate`).

### ۶. `tunnelManager.ts` (سازنده اسکریپت‌های تونل‌زنی ایران-خارج)
تولید اسکریپت‌های شل (Bash) یکپارچه برای راه‌اندازی پل ارتباطی بین سرور ایران و خارج:
1. **Gost v3 Relay:** ایجاد تونل امن WebSocket رمزنگاری شده (`relay+mws`).
2. **Rathole:** تانل فوق سریع بر پایه زبان Rust و پیکربندی Toml.
3. **ICMP Ping Tunnel:** عبور ترافیک پروتکل‌های TCP/UDP از طریق پکت‌های Ping (ICMP) جهت دور زدن قطعی‌های کامل اینترنت.
4. **White DNS Tunnel (dnstt):** عبور ترافیک از طریق درخواست‌های DNS روی پورت ۵۳ دامنه‌های سفید.
5. **IPv6 / Native NAT Forwarding:** فوروارد بومی پکت‌ها با `iptables` و فعال‌سازی `ip_forward`.

### ۷. `telegramBot.ts` (ربات تلگرام جامع ادمین و کلاینت)
ربات تلگرام کاملاً با متد Polling و یک ماشین وضعیت (State Machine) تعاملی برای ویزاردهای چندمرحله‌ای پیاده شده است:
- **امکانات ادمین:**
  - ورود با پسورد ادمین (`WAIT_ADMIN_PASS`).
  - ویزارد ساخت اینباند: دریافت نام، پورت، سکیوریتی (REALITY/TLS)، انتخاب SNI، محدودیت حجم، روزهای انقضا و تعداد دستگاه با دکمه‌های شیشه‌ای.
  - پایش و سوئیچ زنده SNI به صورت دستی.
  - خاموش/روشن کردن کلودفلر WARP و رجیستر مجدد.
  - تست آنلاین دامنه‌های SNI.
  - ساخت اسکریپت تونل با دریافت آی‌پی‌های سرور.
  - بکاپ‌گیری فوری و بازگردانی خودکار دیتابیس به محض دریافت فایل دیتابیس آپلود شده توسط ادمین در چت.
- **امکانات کلاینت:**
  - استعلام آنی حجم باقی‌مانده و تاریخ انقضا با کلیک روی دکمه «وضعیت حساب من».
  - دریافت لینک سابسکریپشن و ورود به وب‌صفحه اختصاصی کاربر.

---

## 8. تحلیل فرانت‌اند و رابط کاربری (`frontend/src/`)

فرانت‌اند با **Vue 3 Composition API**، **Vite** و **Tailwind CSS** همراه با استایل مدرن سایبرپانک شیشه‌ای (Glassmorphism) طراحی شده است:

### کامپوننت‌های کلیدی:
1. **`App.vue`:**
   - تشخیص هوشمند صفحات عمومی سابسکریپشن کاربر (`/subinfo/:uuid`) و جداسازی آن از پنل ادمین.
   - مدیریت سشن لاگین با هدر `Authorization: Bearer <token>`.
   - سوئیچ زبان لایو و واکنش‌گرا بین فارسی (RTL) و انگلیسی (LTR).
   - ساختار ناوبری و تب‌های ریسپانسیو دسکتاپ و موبایل.

2. **`DashboardView.vue`:**
   - نمایش کارت‌های آمار زنده سخت‌افزار (درصد استفاده CPU، نسبت رم، پینگ لحظه‌ای شبکه، آپ‌تایم سرور).
   - جدول ماتریس پروتکل‌ها شامل وضعیت VLESS Reality، Packet Fragment و Gost Tunnel.

3. **`InboundsView.vue`:**
   - پنل پیشرفته تست زنده SNI با تفکیک دسته‌بندی‌ها (دامنه‌های بانکی شاپرک، CDN ابر آروان، سوپراپ‌ها و دامنه‌های بین‌المللی).
   - لیست اینباندهای تعریف‌شده با قابلیت سوئیچ فعال/غیرفعال، دریافت لینک‌های اتصال، بارکد و حذف.
   - بنر هشدار و تریگر اجرای دستی دیمن Auto-Failover SNI.

4. **`UsersView.vue`:**
   - جدول کامل کاربران با نمایش میزان مصرف به گیگابایت، درصد مصرف، سقف دستگاه، تاریخ انقضا و وضعیت اکانت.
   - ویزارد ساخت کاربر جدید و ویرایش مشخصات.
   - مودال‌های دانلود مستقیم فایل‌های Sing-box و Clash و کپی لینک سابسکریپشن.

5. **`SettingsView.vue`:**
   - فرم تنظیمات توکن ربات تلگرام و آیدی عددی چت ادمین.
   - کارت مدیریت جامع خروجی Cloudflare WARP (سوییچ مود ALL/SANCTIONED، مشاهده IPv4/IPv6 اختصاصی و رجیستر اکانت).
   - کارت مدیریت بکاپ‌گیری دیتابیس با دکمه‌های دانلود محلی و ارسال به تلگرام.

6. **`SubUserView.vue`:**
   - پورتال مستقل برای مشترکین جهت مشاهده وضعیت اتصال خود با ورود به آدرس `/subinfo/:uuid`.
   - نمایش بارکد QR سابسکریپشن، نمودار پیشرفت مصرف حجم، روزهای باقی‌مانده و دکمه‌های کپی مستقیم کانفیگ‌ها متناسب با اپراتور کاربر (همراه اول، ایرانسل، رایتل و مخابرات).

7. **`i18n.ts`:**
   - دیکشنری کامل تمام متون و برچسب‌های پنل به دو زبان فارسی و انگلیسی همراه با تغییر جهت خودکار صفحه (`dir="rtl"` و `dir="ltr"`).

8. **`utils/clipboard.ts`:**
   - کپی هوشمند متن در کلیپ‌بورد با استفاده از `navigator.clipboard` و فال‌بک امن بر پایه `document.execCommand` برای پروتکل‌های غیرامن HTTP و دامنه‌های بدون SSL.

---

## 9. تحلیل اسکریپت‌های نصب و استقرار (`install.sh`, `install.ps1`, `uninstall.sh`)

### الف) اسکریپت لینوکس (`install.sh`):
1. **بررسی دسترسی Root:** اطمینان از اجرای اسکریپت با مجوز دسترسی کامل.
2. **راه‌اندازی تعاملی (Interactive Setup):** پرسیدن نام کاربری، پسورد ادمین و پورت پنل از کاربر در صورت اتصال به TTY (با مقادیر پیش‌فرض `admin`, `nyx2026!`, `3080`).
3. **نصب وابستگی‌ها:** نصب خودکار `git`, `unzip` و مخزن `Node.js 20.x LTS`.
4. **کلون پروژه در `/opt/nyx`:** دریافت سورس‌کد و یا به‌روزرسانی پروژه در صورت وجود نسخه قبلی.
5. **دانلود و قرار دادن باینری Xray-core:** دانلود آخرین پکیج متناسب با معماری سیستم (`linux-64` یا `linux-arm64-v8a`) و اعمال مجوز `chmod +x`.
6. **بیلد پروژه:** اجرای `npm install`، `npx prisma db push` و `npm run build` برای بک‌اند و فرانت‌اند به منظور تولید فایل‌های استاتیک در پوشه `dist`.
7. **ایجاد سرویس Systemd (`/etc/systemd/system/nyx.service`):** تنظیم استارت‌آپ خودکار با لینوکس، ریستارت اتوماتیک در صورت بروز خطا و تزریق متغیرهای محیطی `.env`.
8. **پاکسازی پورت‌ها:** متوقف کردن وب‌سرورهای اشغال‌کننده پورت ۴۴۳ (مانند Nginx, Apache, Caddy) و فعال‌سازی `net.ipv4.ip_forward=1` در کرنل لینوکس.

### ب) اسکریپت ویندوز (`install.ps1`):
1. بررسی دسترسی Administrator در پاورشل.
2. نصب پروژه در مسیر `C:\Nyx`.
3. دانلود خودکار Node.js LTS برای ویندوز (MSI Installer) در صورت عدم نصب.
4. دانلود باینری `xray.exe` برای ویندوز ۶۴ بیتی.
5. ساخت فایل `.env` و بیلد پروژه‌های بک‌اند و فرانت‌اند.
6. ایجاد یک سرویس از نوع **Windows Scheduled Task** به نام `NyxPanelService` که در زمان بوت ویندوز با دسترسی `SYSTEM` به صورت خودکار اجرا می‌شود.

### ج) اسکریپت حذف (`uninstall.sh`):
1. توقف و حذف سرویس `nyx.service` از سیستم‌دی.
2. آزادسازی پورت‌های ۳۰۸۰، ۳۰۰۰، ۱۰۰۸۵ و بستن پروسه‌های Node و Xray.
3. پاکسازی کامل دایرکتوری `/opt/nyx` و فایل‌های موقت `/tmp/nyx*`.

---

## 10. جریان‌های داده و ارتباطات فنی (Data Flows)

```
[ کلاینت / کاربر نهایی ]
       │
       │ ۱. درخواست اتصال VPN با پروتکل VLESS + REALITY
       ▼
[ سرور Nyx: پورت 443 ]
       │
       │ ۲. عبور از فیلترینگ با Packet Fragment و فریب SNI (بانک سپه / ابرآروان)
       ▼
[ هسته Xray-Core ]
       │
       ├─► ۳. مسیر عادی اینترنت ──► [ اینترنت آزاد (Freedom Outbound) ]
       │
       └─► ۴. سایت‌های تحریمی / OpenAI ──► [ تونل Cloudflare WARP WireGuard ] ──► [ ChatGPT / Netflix ]

-----------------------------------------------------------------------------------------------------

[ ادمین پنل / ربات تلگرام ]
       │
       │ ۵. ایجاد کاربر جدید / تغییر SNI اینباند
       ▼
[ Node.js API Server ]
       │
       ├─► ۶. ذخیره اطلاعات کاربر در دیتابیس SQLite
       │
       ├─► ۷. تولید فایل کانفیگ جدید config.json
       │
       └─► ۸. ریستارت زنده هسته Xray و اعمال آنی روی اتصالات
```

---

## 11. نکات امنیتی، ضد فیلترینگ و دور زدن DPI اپراتورها

1. **مکانیزم REALITY و عدم نیاز به دامنه شخصی:**
   در روش‌های سنتی TLS، فیلترچی با بررسی گواهی SSL دامنه شما می‌توانست IP سرور را مسدود کند. در REALITY، کلاینت با کلید عمومی سرور هندشیک را آغاز می‌کند و به نظر سیستم مانیتورینگ شبکه می‌رسد که کاربر در حال اتصال به یک سایت معتبر خارجی یا بانکی داخلی است.
2. **پروتکل Vision Flow (`xtls-rprx-vision`):**
   حذف کامل پترن‌های رمزنگاری لایه‌ای که به سیستم‌های یادگیری ماشین DPI اجازه شناسایی تونل‌های وی‌پی‌ان را می‌داد.
3. **تکنیک فرگمنتاسیون (Packet Fragmentation):**
   بسته‌های ClientHello در هندشیک اولیه به قطعات کوچک ۱۰۰ تا ۲۰۰ بایتی شکسته می‌شوند تا فایروال اپراتور امکان خواندن فیلد SNI و بازرسی عمیق پکت (Deep Packet Inspection) را نداشته باشد.
4. **سوئیچینگ هوشمند SNI (Auto-Failover):**
   در زمان اختلالات شدید سراسری (مثل ملی شدن اینترنت)، اتصال با جایگزینی خودکار دامنه‌های لیست سفید بدون قطعی باقی می‌ماند.
5. **مخفی‌سازی IP سرور با Cloudflare WARP:**
   ترافیک سرور شما به صورت دوطرفه از شبکه کلودفلر رد می‌شود که مانع از بلاک شدن رنج IP سرور شما توسط فایروال‌های هدف می‌گردد.

---
**پایان مستند جامع تحلیل پروژه Nyx Panel v2.2.0**  
*تهیه شده جهت استفاده توسعه‌دهندگان، مهندسین DevOps و تحلیل‌گران امنیت شبکه.*
